# Current Feature: Auth Credentials - Email/Password Provider

## Status

In Progress

## Goals

- Add a Credentials provider for email/password authentication, alongside the existing GitHub provider
- Support user registration via a new `POST /api/auth/register` API route (name, email, password, confirmPassword)
- Passwords hashed with `bcryptjs` (already installed); add a `password` field to the `User` model via a Prisma migration if not already present
- Registration route validates that passwords match, checks for an existing user, hashes the password, and creates the user
- Users can sign in with email/password via the default `/api/auth/signin` page and land on `/dashboard`, with GitHub OAuth continuing to work unchanged

## Notes

- Reference spec: `context/features/auth-phase-2-spec.md`
- Split config pattern: `src/auth.config.ts` gets a Credentials provider with an edge-safe `authorize: () => null` placeholder; `src/auth.ts` overrides it with the real bcrypt-based `authorize()` validation logic (this is Phase 2 of the auth work, following `feature/auth-phase-1`'s GitHub-only setup)
- Testing: register via curl, then sign in through `/api/auth/signin`, confirm redirect to `/dashboard`, and confirm GitHub OAuth still works
- Reference: Auth.js Credentials provider docs — https://authjs.dev/getting-started/authentication/credentials

## History

- **Project Bootstrap** - Next.js 16.2.10 app scaffolded via create-next-app (`package.json`, `src/app/`)
- **TypeScript Configuration** - Strict mode, bundler module resolution, `@/*` path alias (`tsconfig.json`)
- **Tailwind CSS v4 Setup** - CSS-first Tailwind via `@import "tailwindcss"` and `@tailwindcss/postcss` plugin (`src/app/globals.css`, `postcss.config.mjs`)
- **React Compiler** - Enabled in Next.js config (`next.config.ts`)
- **ESLint Setup** - ESLint 9 flat config with `eslint-config-next` (`eslint.config.mjs`)
- **Root Layout & Placeholder Homepage** - Basic root layout with DevStash metadata (`src/app/layout.tsx`); minimal `<h1>DevStash</h1>` placeholder at `/` (`src/app/page.tsx`)
- **Dashboard UI Phase 1** - ShadCN UI initialized with `next-themes` and `lucide-react`. Built the main dashboard layout at `/dashboard` enforcing dark mode by default. Included a `TopBar` with dummy search and new item buttons, alongside basic placeholders for the `Sidebar` and `Main` content areas.
- **Dashboard UI Phase 2** - Built collapsible sidebar with Types, Favorite Collections, Recent Items, and User Avatar using mock data. Added mobile drawer via Shadcn `Sheet`. Integrated `DashboardShell` client component for layout state management. (`src/components/layout/sidebar.tsx`, `mobile-sidebar.tsx`, `dashboard-shell.tsx`)
- **Dashboard UI Phase 3** - Built main dashboard components (`DashboardStats`, `PinnedItems`, `RecentCollections`, `RecentItemsList`) and assembled them into the `/dashboard` page using a responsive grid layout. (`src/components/dashboard/*.tsx`, `src/app/(dashboard)/dashboard/page.tsx`)
- **Dashboard Spec & Styling Fixes** - Rendered the missing `DashboardStats` cards on `/dashboard`; sidebar user card and type counts now derived from mock data instead of hardcoded values; extracted shared item-type icon/color constants (`src/lib/constants/item-types.ts` + `@theme` type colors in `globals.css`); replaced hardcoded `zinc-*`/hex colors and inline styles with semantic theme tokens across layout and dashboard components; pinned item cards use per-type left-border colors; synced context docs.
- **Prisma + Neon PostgreSQL Setup** - Installed Prisma 7 (`prisma`, `@prisma/client`, `@prisma/adapter-pg`, `pg`, `dotenv`) and wired it up for Prisma 7's breaking changes: `prisma.config.ts` (root) now owns the datasource URL/migrations/seed config instead of the `schema.prisma` datasource block; generator uses the Rust-free `provider = "prisma-client"` with an explicit `output` into `src/generated/prisma` (gitignored); a `PrismaPg` driver adapter wires the generated client to Neon via `pg`. Added the full schema from `context/project-overview.md` (`prisma/schema.prisma`) including NextAuth models (Account, Session, VerificationToken), indexes, and cascade deletes. Added a hot-reload-safe client singleton (`src/lib/prisma.ts`), seed script for the 7 system item types (`prisma/seed.ts`), and a `postinstall: prisma generate` script. Ran `prisma migrate dev --name init` against a live Neon database and seeded successfully (verified 7 `ItemType` rows); `npm run build` passes.
- **Demo Data Seeding** - Extended `prisma/seed.ts` (reference: `context/features/seed-spec.md`) to seed a demo user (`demo@devstash.io` / "Demo User", password `12345678` hashed with `bcryptjs` at 12 rounds, `isPro: false`) alongside the existing system item types. Added `bcryptjs` + `@types/bcryptjs` as dependencies. Seeded 5 demo collections owned by the user — React Patterns (3 TS snippets), AI Workflows (3 prompts), DevOps (1 snippet, 1 command, 2 real doc links), Terminal Commands (4 commands), Design Resources (4 real links) — 18 items total, linked to their collections via `ItemCollection`. Seeding is idempotent (find-or-create per collection/item, `upsert` on the `ItemCollection` composite key), verified by running `prisma db seed` twice with no duplicate rows created; `npm run build` passes.
- **Dashboard Collections Wired to Database** (`feature/dashboard-collections`, reference: `context/features/dashboard-collections-spec.md`) - Replaced the mock-data-backed recent collections grid with real data. Added `src/lib/db/collections.ts` (`getDashboardCollections()`) fetching the demo user's collections from Neon via Prisma — joins items → item type and computes, per collection, the item count, the most-used item type (for the card's left-border color), and the distinct type names present (for the small type-icon row). `src/components/dashboard/recent-collections.tsx` converted to an async server component calling this function directly (no client fetch, no API route); item list underneath collections intentionally deferred. Also swapped an arbitrary `min-h-[140px]` for the canonical `min-h-35` Tailwind utility, and set `sslmode=verify-full` in `.env` to silence a `pg` driver deprecation warning surfaced once this component started querying the database. Verified via `npm run build`, `npm run lint`, and a dev-server HTML check confirming all 5 seeded collections render with correct item counts and correctly-derived border colors (e.g. DevOps → `link`, its most common type).
- **Dashboard Items Wired to Database** (`feature/dashboard-items`, reference: `context/features/dashboard-items-spec.md`) - Replaced the mock-data-backed pinned/recent items and stats cards with real data. Added `src/lib/db/items.ts` with `getPinnedItems()`, `getRecentItems()` (each fetching the demo user's items from Neon via Prisma, joined to item type and tags, ordered by `createdAt` desc, capped at 10), and `getDashboardStats()` (total/favorite counts for items and collections via `prisma.count`). `src/components/dashboard/pinned-items.tsx`, `recent-items-list.tsx`, and `dashboard-stats.tsx` converted to async server components calling these functions directly; the "no pinned items → render nothing" behavior carried over unchanged. Since no seeded item had `isPinned`/`isFavorite`/tags set, extended `prisma/seed.ts` (`SeedItemCommon` type) to pin "useDebounce Hook" and "Refactoring Assistance Prompt", favorite those plus "Deploy to Production", and attach a few demo tags via `connectOrCreate`; existing seeded rows are now updated (not just created) on reseed so these fields apply idempotently. Verified via `npm run build`, `npm run lint`, `npx prisma db seed`, and a dev-server HTML check confirming both pinned items render with their tags, stat cards show the correct live counts (18 items, 5 collections, 2 favorite items, 0 favorite collections), and the recent-items 10-item cap correctly excludes older non-pinned items.
- **Stats & Sidebar Wired to Database** (`feature/stats-sidebar`, reference: `context/features/stats-sidebar-spec.md`) - Wired the sidebar to real data (dashboard stats were already live from the prior feature). Added `getItemTypesWithCounts()` to `src/lib/db/items.ts` (all 7 system types in fixed order, each with the demo user's per-type item count via `groupBy`). Refactored `src/lib/db/collections.ts` to extract a shared `toDashboardCollection` mapper and added `getSidebarCollections()`, splitting the user's collections into `favorites`/`recents`. Added a `dot` class per item type to `src/lib/constants/item-types.ts` for a colored circle on recent collections (based on most-used type), plus `ITEM_TYPE_LABELS`/`getTypeLabel()` for pluralized type names in the sidebar (Snippets, Prompts, Commands, Notes, Files, Images, Links). Since Prisma can't be called from the client-component sidebar tree, `src/app/(dashboard)/layout.tsx` became an async Server Component that fetches item types + sidebar collections and threads them down as props through `dashboard-shell.tsx` → `top-bar.tsx` → `mobile-sidebar.tsx` → `sidebar.tsx` (desktop sidebar and mobile Sheet drawer share the same data). `sidebar.tsx` type links now go to `/items/[name]` with live counts, favorite collections keep the star icon, recent collections show the colored dot, and a new "View all collections" link points to `/collections`. Also fixed the Types/Collections section headers, which had a static decorative chevron with no click handler — converted to real `<button>`s with `typesOpen`/`collectionsOpen` state that rotate the chevron and collapse/expand their lists. Verified via `npm run build`, `npm run lint`, and dev-server HTML checks confirming correct type counts (4/3/5/0/0/0/6 matching the 18 seeded items), collection dots/labels, working `/items/[name]` and `/collections` links, and the toggle buttons rendering as real `<button>` elements.
- **Add Pro Badge to Sidebar** (`feature/add-pro-badge-sidebar`, reference: `context/features/add-pro-badge-sidebar.md`) - Added a small "PRO" badge to the Files and Images entries in the sidebar's Types list (`src/components/layout/sidebar.tsx`), using the existing ShadCN `Badge` component (`variant="outline"`, `h-4 px-1.5 text-[9px]`, muted-foreground) for a clean, subtle look. Styling-only — no pro-gating logic exists yet (`requirePro()` is still planned, not implemented). Since `MobileSidebar` reuses the same `Sidebar` component, the badge applies to both desktop and mobile automatically. Verified via `npm run build`.
- **Quick-Win Cleanups from Code Review** - Fixed all 5 low-risk findings from the code-reviewer agent's full-codebase review. (1) Added a shared cached user lookup `getDemoUser()` in a new `src/lib/db/user.ts` (React `cache()`-wrapped `prisma.user.findUnique({ where: { email }, select: { id: true } })`), and switched every call site in `src/lib/db/items.ts` (`getPinnedItems`, `getRecentItems`, `getDashboardStats`, `getItemTypesWithCounts`) and `src/lib/db/collections.ts` (`getDashboardCollections`, `getSidebarCollections`) from a duplicated `prisma.user.findUnique` to `getDemoUser()`, then querying `prisma.item`/`prisma.collection` directly with the resolved `userId` instead of nested `user.findUnique` selects — collapses up to 6 redundant per-request user lookups on a single `/dashboard` load into 1. (2) `getSidebarCollections` now takes a `limit` (defaults to a new shared `COLLECTIONS_LIMIT = 6` constant, matching `getDashboardCollections`) and uses `_count: { select: { items: true } }` for the item count plus a bounded `items: { take: 1, ... }` nested include for an approximate primary-type dot (sidebar only renders `itemCount` + `primaryType`, never the full `typeNames` list, which is left as `[]` for sidebar collections — `getDashboardCollections` is unchanged and still computes exact `typeNames`/`primaryType` from the full item list). (3) Added `!.env.example` to `.gitignore` (after the blanket `.env*` line) and populated the previously-untracked `.env.example` with a documented placeholder `DATABASE_URL`. (4) Changed `mockUser.isPro` in `src/lib/mock-data.ts` from `true` to `false` to match `prisma/seed.ts`'s seeded demo user. (5) Deleted the dead `mockItemTypes`, `mockCollections`, and `mockItems` exports from `src/lib/mock-data.ts` (confirmed zero remaining imports anywhere in `src/` via grep first); `mockUser` was kept as-is since `src/components/layout/sidebar.tsx` still uses it live. Implemented on `fix/quick-win-cleanups`, verified via `npm run build` and `npm run lint` (both pass; one pre-existing unrelated ESLint warning in `top-bar.tsx`), committed, and merged to `main`.
- **Auth Setup - NextAuth + GitHub Provider (Phase 1)** (`feature/auth-phase-1`, reference: `context/features/auth-phase-1-spec.md`) - Installed `next-auth@5.0.0-beta.31` and `@auth/prisma-adapter`. Set up the split config pattern for edge compatibility: `src/auth.config.ts` (GitHub provider only, edge-safe) and `src/auth.ts` (full `NextAuth()` call using the existing Prisma singleton from `src/lib/prisma.ts` via `PrismaAdapter`, `session: { strategy: "jwt" }`, and a `session` callback copying `token.sub` into `session.user.id` since the adapter's database id isn't otherwise exposed under the JWT strategy). Added the route handler at `src/app/api/auth/[...nextauth]/route.ts` (re-exports `GET`/`POST` from `handlers`) and `src/types/next-auth.d.ts` (module augmentation adding `user.id` to `Session`). Added `src/proxy.ts` — named `proxy` export (not default), `matcher: ["/dashboard/:path*"]`, redirects unauthenticated requests to `/api/auth/signin?callbackUrl=...` — using NextAuth's default sign-in page (no custom `pages.signIn`). Documented `AUTH_SECRET`/`AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` in `.env.example`. No pro-gating or custom auth pages — reserved for phases 2/3 (`auth-phase-2-spec.md`, `auth-phase-3-spec.md`), not implemented here. Verified via `npm run build`, `npm run lint`, and a live Playwright run against the dev server: unauthenticated `/dashboard` redirected to the default sign-in page, and clicking "Sign in with GitHub" redirected to GitHub's OAuth authorize URL with the correct `client_id`/PKCE/callback params (stopped short of entering real credentials). Committed and merged to `main`.
